export type SearchTab = 'post' | 'community' | 'user'

export type SearchTabs = Array<SearchTab>

export type DrawerSection = 'feed' | 'feeds' | 'communities' | 'users'

export type DrawerSections = Array<DrawerSection>
