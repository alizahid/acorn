import { MenuItemButton } from './item/button'
import { MenuItemOptions } from './item/options'
import { MenuItemSwitch } from './item/switch'
import { MenuLabel } from './label'
import { MenuRoot } from './root'
import { MenuSeparator } from './separator'

export const Menu = {
  Button: MenuItemButton,
  Label: MenuLabel,
  Options: MenuItemOptions,
  Root: MenuRoot,
  Separator: MenuSeparator,
  Switch: MenuItemSwitch,
}
